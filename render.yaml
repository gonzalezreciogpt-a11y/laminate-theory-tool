services:
  - type: web
    name: laminate-theory-tool
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_BASE_URL
        sync: false
      - key: LOG_LEVEL
        value: info
